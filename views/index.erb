<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Picaflic</title>
        <!-- Include normalize to get all of my elements behaving the same and following some basic best practices -->
        <link rel="stylesheet" href="../css/normalize.css">
        <link rel="stylesheet" href="../css/app.css">
        <!-- Include my frontend JS functionality -->
        <script src="../js/app.js"></script>
    </head>
    <body>
        <div class="hero">
            <h1>Picaflic</h1>

            <form name="search-form">
                <input id="query" type="text" placeholder="Search on IMDB"/>
                <input  type="submit" value="Search" id="submit"/>
            </form>
        </div>
        <div class="container">
            <div id="search-results"></div>

            <div class="favourites">
                <p><a href="javascript:void(0)" id="favourite-film-toggle">My Favourites</a></p>

                <ul id="favourites-container" style="display:none">
                    <% @faves.each do |f| %>
                        <li><%= f["name"] %></li>
                    <% end %>
                </ul>
            </div>
        </div>
    </body>
</html>